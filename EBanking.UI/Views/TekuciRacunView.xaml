<Window x:Class="EBanking.UI.Views.RacunView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EBanking.UI.Views"
        mc:Ignorable="d"
        DataContext="{Binding RacunView, Source={StaticResource Locator}}"
        Title="{Binding Model.Title}" Height="600" Width="900" >
    <Grid>
        <Label Content="Dobro dosli" HorizontalAlignment="Left" Height="25" Margin="10,10,0,0" VerticalAlignment="Top" Width="85"/>
        <Label Content="Vasi racuni" HorizontalAlignment="Left" Height="25" Margin="130,82,0,0" VerticalAlignment="Top" Width="86"/>
        <Label Content="{Binding Model.ImeKorsnika}" HorizontalAlignment="Left" Height="24" Margin="95,11,0,0" VerticalAlignment="Top" Width="154"/>
        <Label Content="{Binding Model.IzabranRacun.BrojRacuna, Mode=TwoWay}" HorizontalAlignment="Left" Height="24" Margin="400,76,0,0" VerticalAlignment="Top" Width="154"/>
        <DataGrid ItemsSource="{Binding Model.Racuni, Mode=TwoWay}" SelectedItem="{Binding Model.IzabranRacun, Mode=TwoWay}" Grid.Row="1" MaxHeight="300" FontSize="15" IsSynchronizedWithCurrentItem="True" Name="GridRacuni" AutoGenerateColumns="False" CanUserAddRows="False" Margin="10,115,564,372" VerticalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Broj Racuna" IsReadOnly="true" Binding="{Binding BrojRacuna}" Width="1.5*"></DataGridTextColumn>
                <DataGridTextColumn Header="Tip racuna" IsReadOnly="true" Binding="{Binding Tip}" Width="1.5*"></DataGridTextColumn>
                <DataGridTextColumn Header="Valuta" IsReadOnly="true" Binding="{Binding Valuta}" Width="1.5*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Novo placanje" Command="{Binding PlacanjeCommand}" HorizontalAlignment="Left" Margin="734,521,0,0" VerticalAlignment="Top" Height="28" Width="85"/>
        <Label Content="{Binding Model.IzabranRacun.Balans}" HorizontalAlignment="Left" Margin="542,115,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
